import { styled } from "styled-components";

export const Container = styled.main`
  width: max-content;
  margin: 0 auto;
  padding-top: 2.4rem;

  display: flex;
  flex-direction: column;
  align-items: center;
`;
